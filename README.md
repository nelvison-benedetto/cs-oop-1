## C# OOP

..., constructors(also partial), properties (e.g.protected string Name { get; set; } ), sealed, cast, .Equals() vs ==, override bool Equals(object? obj), override string ToString(), override int GetHashCode(), 
records, encapsulation, inheritance, polymorphism, abstraction, interfaces